PUSH KEY CLEAR

SELECT VT
SET ORDER TO 1
SET RELATION TO 
SELECT VTUPL
DO  FORM VTUPL
SELECT VTUPL
SET RELATION to
MPIB=PIB
SELECT VT
SET ORDER TO 1
SEEK mpib
IF FOUND()
   mrec=RECNO()
ELSE
   mrec=0
endif      
USE &KVTGDE IN 0 ALIAS VTGDE EXCLUSIVE
SELECT VTGDE
REPLACE TPOLJE WITH MREC
MTPOLJE=TPOLJE
USE

CLOSE ALL
VTA.release
POP KEY
DO VTa WITH MTPOLJE
