PUSH KEY CLEAR
SELECT ROB
SET ORDER TO 8
MPODROB=PODROB
SET RELATION TO
AMREC=RECNO()

DO FORM TVKARTG WITH MPODROB

SELECT ROB
IF RECCOUNT()>0
   GOTO AMREC
ENDIF
ROBA0.GRD0.RECORDSOURCE='ROB'
ROBA0.GRD0.SETFOCUS
POP KEY
ROBA0.REFRESH
