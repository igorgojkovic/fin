PUSH KEY CLEAR
SELECT PRONROP
MREC=RECNO()
USE
USE PRONROP IN 0 EXCLUSIVE ORDER 1
SELECT PRONROP
DELETE ALL FOR RECNO()=MREC
PACK
USE
USE PRONROP IN 0 ORDER 1
SELECT PRONROP
PRONROP.RELEASE
POP KEY
KEYBOARD '{ENTER}'

