PUSH KEY CLEAR

SELECT VT
SET ORDER TO 1
SET RELATION TO 
SELECT VTUPLD
DO  FORM VTUPLD
SELECT VTUPLD
SET RELATION to
MPIB=PIB
SELECT VT
SET ORDER TO 1
SEEK mpib
IF FOUND()
   mrec=RECNO()
ELSE
   mrec=0
endif      
USE &KVTGDE IN 0 ALIAS VTGDE EXCLUSIVE
SELECT VTGDE
REPLACE TPOLJE WITH MREC
MTPOLJE=TPOLJE
MTMENI=TMENI
USE
CLOSE ALL
VTA.release
POP KEY
DO VTA WITH MTPOLJE