PUSH KEY CLEAR
SELECT ld
mredni=recno()
SELECT LDDD0
MMESEC=MESEC
SELECT LD 
USE
USE LDDD EXCLU ALIAS LD IN 0
SELECT LD
IF RECCOUNT()>0
goto mredni
DELETE
PACK
ENDIF
*CLOSE ALL TABLES
POP KEY
LDDD.RELEASE
KEYBOARD '{ENTER}'

