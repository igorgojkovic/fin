PUSH KEY CLEAR
SELECT ROB
SET ORDER TO 1
MRSIF=RSIF
SET RELATION TO
AMREC=RECNO()

DO FORM TVKARTN WITH MRSIF

SELECT ROB
IF RECCOUNT()>0
   GOTO AMREC
ENDIF
ROBA0.GRD0.RECORDSOURCE='ROB'
ROBA0.GRD0.SETFOCUS
POP KEY
ROBA0.REFRESH
